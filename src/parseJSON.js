// this is what you would do if you were one to do things the easy way:
// var parseJSON = JSON.parse;

// but you're not, so you'll write it from scratch:
var parseJSON = function(json) {
  // your code goes here
  
};



/*
getType(str, index) {
  if (str === { ) {
    makeObject (json.slice(index))
  }
  if (str == [){
   makeArray();
  }
  makeValue();
}


makeObject(str) {
  var key = '';
  build a string until we see :
  add this as key name
  run getType on str after : and set that equal to key name
  key = getType(obj[key])
  
}



{a:0, b: {c:0}, d: {e:0}}

*/